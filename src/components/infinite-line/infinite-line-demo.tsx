import React from "react";
import { InfiniteLine } from "./infinite-line";
import styles from "./infinite-line-demo.module.css";

export function InfiniteLineDemo() {
  return <InfiniteLine className={styles.root} />;
}
